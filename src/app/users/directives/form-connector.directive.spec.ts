import { FormConnectorDirective } from './form-connector.directive';

describe('FormConnectorDirective', () => {
  it('should create an instance', () => {
    const directive = new FormConnectorDirective();
    expect(directive).toBeTruthy();
  });
});
